<?xml version="1.0" encoding="utf-8"?>
<Configuration>

    <!--These are some of the loggers you can enable. 
        There are several more you can find in the documentation. 
        Besides this log4j configuration, you can also use Java VM environment variables
        to enable other logs like network (-Djavax.net.debug=ssl or all) and 
        Garbage Collector (-XX:+PrintGC). These will be append to the console, so you will 
        see them in the mule_ee.log file. -->

    <Appenders>

        <RollingFile name="ApplicationLoggerAppender"
		fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}physical-inventory-process-api.log" 
                filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}physical-inventory-process-api-%i.log">
		<PatternLayout pattern="%d [%t] %-5p %c - %m%n" />
		<SizeBasedTriggeringPolicy size="50 MB" />
            	<DefaultRolloverStrategy max="10"/>
        </RollingFile>

	<RollingFile name="AuditLoggerAppender"
		fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}audit${sys:file.separator}physical-inventory-process-api_audit.log"
		filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}audit${sys:file.separator}physical-inventory-process-api_audit-%i.log">
		<PatternLayout pattern="%d [%t] %-5p %c - %m%n" />
		<SizeBasedTriggeringPolicy size="100 MB" />
		<DefaultRolloverStrategy max="10" />
	</RollingFile>

    </Appenders>

	<Loggers>

		<!-- Http Logger shows wire traffic on DEBUG. -->
		<!--AsyncLogger name="org.mule.service.http.impl.service.HttpMessageLogger" 
			level="DEBUG" / -->
		<AsyncLogger name="org.mule.service.http" level="WARN" />
		<AsyncLogger name="org.mule.extension.http" level="WARN" />

		<!-- Mule logger -->
		<AsyncLogger
			name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor"
			level="INFO" />

		<!-- Http Logger shows wire traffic on DEBUG -->
		<AsyncLogger
			name="org.mule.module.http.internal.HttpMessageLogger" level="WARN" />

		<!-- JDBC Logger shows queries and parameters values on DEBUG -->
		<AsyncLogger name="com.mulesoft.mule.transport.jdbc"
			level="WARN" />

		<!-- CXF is used heavily by Mule for web services -->
		<AsyncLogger name="org.apache.cxf" level="WARN" />

		<!-- Apache Commons tend to make a lot of noise which can clutter the log -->
		<AsyncLogger name="org.apache" level="WARN" />

		<!-- Reduce startup noise -->
		<AsyncLogger name="org.springframework.beans.factory"
			level="WARN" />

		<!-- Mule classes -->
		<AsyncLogger name="org.mule" level="INFO" />
		<AsyncLogger name="com.mulesoft" level="INFO" />

		<!-- Reduce DM verbosity -->
		<AsyncLogger name="org.jetel" level="WARN" />
		<AsyncLogger name="Tracking" level="WARN" />

		<!-- Custom Configuration -->
		<logger name="ApplicationLogger" level="INFO">
			<AppenderRef ref="ApplicationLoggerAppender" />
		</logger>

		<logger name="AuditLogger" level="INFO" additivity="false">
			<AppenderRef ref="AuditLoggerAppender" />
		</logger>

		<AsyncRoot level="INFO">
			<AppenderRef ref="ApplicationLoggerAppender" />
		</AsyncRoot>

	</Loggers>
</Configuration>

